name: .NET TEMPLATE
on:
  pull_request:
    branches: [ "main"]
  workflow_dispatch:  
  
jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - name: Chamando o Workflow Reutilizável de A
        uses: Limasjg-Fly/.github/.github/workflows/sonar-default.yml@main
        with:
          sonar_token: ${{ secrets.SONAR_TOKEN }}
